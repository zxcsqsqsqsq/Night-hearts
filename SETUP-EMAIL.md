# 📧 Настройка отправки заявок на почту

## Что было добавлено

✅ Новая вкладка "Админы" с формой подачи заявки  
✅ Выбор типа админа (Обычный админ, Монтажер)  
✅ Отправка заявок на вашу почту  
✅ Автоматическое письмо подтверждения пользователю  

## 🔧 Инструкция по настройке

### Шаг 1: Подготовка файла PHP

Файл `send-email.php` уже создан. Откройте его и найдите эту строку:

```php
$recipientEmail = 'your-email@gmail.com'; // ИЗМЕНИТЕ НА ВАШУ ПОЧТУ!
```

**Замените `your-email@gmail.com` на вашу реальную почту**, например:
```php
$recipientEmail = 'admin@mysite.com';
```

### Шаг 2: Загрузка на хостинг

Все файлы нужно загрузить на хостинг, поддерживающий PHP:

```
site/
├── index.html
├── styles.css
├── script.js
├── send-email.php  ← ВАЖНО!
└── README.md
```

### Шаг 3: Проверка отправки писем

Большинство бесплатных хостингов поддерживают функцию `mail()` в PHP. Если письма не приходят, проверьте:

1. **Правильно ли указана почта?** - Проверьте адрес в `send-email.php`
2. **Спам-фильтр?** - Писем могут попадать в папку "Спам"
3. **Хостинг поддерживает PHP?** - Используйте хостинг вроде:
   - 000webhost.com (бесплатный)
   - Infinityfree.net
   - Byethost.com

## 🚀 Альтернативный способ (без PHP)

Если ваш хостинг не поддерживает PHP, используйте **Formspree**:

1. Перейдите на [formspree.io](https://formspree.io)
2. Создайте новую форму и получите код
3. Замените `send-email.php` на ссылку формы Formspree

Пример:
```javascript
// Вместо fetch к send-email.php используйте:
const response = await fetch('https://formspree.io/f/YOUR_FORM_ID', {
    method: 'POST',
    headers: {
        'Accept': 'application/json'
    },
    body: JSON.stringify(formData)
});
```

## 📝 Данные в форме

Форма собирает:
- ✅ Полное имя
- ✅ Email
- ✅ Telegram аккаунт (@username)
- ✅ Тип админа (Обычный админ / Монтажер)
- ✅ Опыт в модерации
- ✅ Причина присоединения

## 📧 Что приходит на почту

**Админу (вам):**
- Красиво отформатированное письмо со всеми данными заявки
- Информация о времени отправки

**Заявителю:**
- Письмо подтверждения получения заявки
- Ссылка на бота в Telegram для связи

## ⚙️ Дополнительные настройки

### Изменить адрес отправителя письма

В файле `send-email.php` найдите:
```php
$headers .= "From: <no-reply@nightheart.site>\r\n";
```

Замените на ваш домен:
```php
$headers .= "From: <no-reply@yourdomain.com>\r\n";
```

### Добавить ещё адреса для получения заявок

Если нужно отправлять заявки сразу нескольким людям:

```php
$recipientEmail = 'admin1@gmail.com, admin2@gmail.com, admin3@gmail.com';
```

## 🐛 Решение проблем

### Письма не приходят
1. Проверьте папку "Спам"
2. Убедитесь, что PHP включен на хостинге
3. Используйте бесплатный хостинг с поддержкой PHP

### Форма не отправляется
1. Откройте консоль браузера (F12)
2. Проверьте наличие ошибок в консоли
3. Убедитесь, что файл `send-email.php` загружен на сервер

### Письма отправляются, но неправильное форматирование
Отредактируйте HTML в файле `send-email.php` в переменной `$emailBody`

## 💡 Советы

- ✉️ Используйте **Gmail** для получения заявок (надежный)
- 🔐 Создайте **отдельную почту** для заявок
- 📱 Добавьте **номер телефона** в письме подтверждения
- 🔔 Используйте **email-уведомления** в почте

---

**Форма полностью готова к использованию!** 🎉
